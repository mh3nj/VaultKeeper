# VaultKeeper v3.0 — Python 3.11+
# Install:  pip install -r requirements.txt

# ── Auto-type ─────────────────────────────────────────────────────────────────
keyboard>=0.13.5            # system-wide keyboard simulation (Linux: needs root or uinput)

# ── QR codes (emergency access) ───────────────────────────────────────────────
qrcode[pil]>=7.4.2          # QR generation for emergency packages
pillow>=10.3.0              # image support for QR / icons

# ── System tray ───────────────────────────────────────────────────────────────
pystray>=0.19.0             # system tray icon

# ── OPTIONAL — remove if not needed ───────────────────────────────────────────
# matplotlib>=3.8.2         # for analytics charts in GUI (large dependency)
# colorama>=0.4.6           # colored terminal output on Windows

# ── NOT included (intentional) ────────────────────────────────────────────────
# requests                  # VaultKeeper is offline-first — no HTTP client
